# mornFront MVP 项目环境变量配置指南
## env.txt - Environment Variables Configuration Guide

### 概述
mornFront 是一个基于 Next.js 的 AI 代码生成平台，支持多种 AI 模型、用户认证、订阅系统和 GitHub 集成。本指南详细说明如何配置所有必需的环境变量。

### 📊 项目特性
- ✅ **多模型 AI 支持**: DeepSeek、OpenAI GPT-4、Anthropic Claude
- ✅ **订阅系统**: 分级订阅（免费版/基础版/专业版/旗舰版）
- ✅ **用户认证**: Supabase + Google/GitHub OAuth
- ✅ **支付集成**: Stripe + PayPal
- ✅ **GitHub 集成**: 一键推送代码到仓库
- ✅ **流式生成**: 实时字符级代码生成
- ✅ **项目管理**: 保存和管理生成的项目

### 📊 当前配置状态
```
DEEPSEEK_API_KEY: 未设置 ❌
NEXT_PUBLIC_SUPABASE_URL: 未设置 ❌
```
*运行 `node test-env.js` 可检查当前配置状态*

---

## 🔑 环境变量配置

### ⚠️ 重要提醒
- 🔴 **标有 🔴 的变量必须替换为你的实际值**
- 🔴 **标有 ** 的占位符文本需要替换**
- ✅ 不标明的变量可以使用默认值

### 1. DeepSeek AI 配置 (必需)
用于基础 AI 代码生成功能，免费版用户可用。

```env
# DeepSeek AI 配置
DEEPSEEK_API_KEY=**sk-your-actual-deepseek-api-key**  # 🔴 必需：DeepSeek API 密钥 (以 sk- 开头)
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_TOKENS=4000
DEEPSEEK_TEMPERATURE=0.7
```

**配置步骤：**
1. 访问 [DeepSeek Platform](https://platform.deepseek.com/)
2. 注册账户 → Dashboard → API Keys
3. 创建新 API Key
4. 复制密钥并替换 `**sk-your-actual-deepseek-api-key**`

### 2. OpenAI 配置 (可选 - 专业版/旗舰版)
用于 GPT-4 和 GPT-4 Turbo 模型。

```env
# OpenAI 配置
OPENAI_API_KEY=**sk-your-openai-api-key**  # 🔴 可选：OpenAI API 密钥 (专业版以上需要)
OPENAI_BASE_URL=https://api.openai.com/v1
```

**配置步骤：**
1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. API Keys → Create new secret key
3. 复制密钥并替换占位符

### 3. Anthropic 配置 (可选 - 专业版/旗舰版)
用于 Claude 3 模型系列。

```env
# Anthropic 配置
ANTHROPIC_API_KEY=**sk-ant-your-anthropic-key**  # 🔴 可选：Anthropic API 密钥 (专业版以上需要)
ANTHROPIC_BASE_URL=https://api.anthropic.com
```

**配置步骤：**
1. 访问 [Anthropic Console](https://console.anthropic.com/)
2. API Keys → Create Key
3. 复制密钥并替换占位符

### 4. Supabase 数据库配置 (推荐)
用于用户认证、订阅管理和数据存储。

```env
# Supabase 数据库配置
NEXT_PUBLIC_SUPABASE_URL=**https://your-project-id.supabase.co**  # 🔴 必需：Supabase 项目 URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=**your-supabase-anon-key**  # 🔴 必需：Supabase 匿名密钥
SUPABASE_SERVICE_ROLE_KEY=**your-service-role-key**  # 🔴 必需：Supabase 服务角色密钥
```

**配置步骤：**
1. 访问 [Supabase Dashboard](https://supabase.com/dashboard)
2. New Project → 填写项目信息
3. Settings → API → 复制以下信息：
   - Project URL → 替换 `NEXT_PUBLIC_SUPABASE_URL`
   - anon public → 替换 `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - service_role → 替换 `SUPABASE_SERVICE_ROLE_KEY`

### 5. Stripe 支付配置 (推荐)
用于国际用户支付，支持一次性订阅。

```env
# Stripe 支付配置
STRIPE_SECRET_KEY=**sk_test_your-stripe-secret-key**  # 🔴 必需：Stripe Secret Key (以 sk_test_ 开头)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=**pk_test_your-publishable-key**  # 🔴 必需：Stripe Publishable Key (以 pk_test_ 开头)
STRIPE_WEBHOOK_SECRET=**whsec_your-webhook-secret**  # 🔴 必需：Stripe Webhook Secret (用于验证 webhook)
```

**配置步骤：**
1. 访问 [Stripe Dashboard](https://dashboard.stripe.com/)
2. Developers → API keys → 复制密钥
3. Webhooks → Add endpoint → URL: `https://yourdomain.com/api/payment/webhook`
4. 复制 webhook secret

### 6. PayPal 支付配置 (可选)
用于 PayPal 支付，支持中国用户。

```env
# PayPal 支付配置
PAYPAL_CLIENT_ID=**your-paypal-client-id**  # 🔴 可选：PayPal Client ID
PAYPAL_CLIENT_SECRET=**your-paypal-client-secret**  # 🔴 可选：PayPal Client Secret
PAYPAL_ENVIRONMENT=sandbox  # sandbox 或 production
PAYPAL_WEBHOOK_ID=**your-paypal-webhook-id**  # PayPal Webhook ID
PAYPAL_SKIP_SIGNATURE_VERIFICATION=false  # 开发环境可设为 true
```

### 7. GitHub OAuth 配置 (可选)
用于一键推送生成的代码到 GitHub 仓库。

```env
# GitHub OAuth 配置
GITHUB_CLIENT_ID=**your-github-client-id**  # 🔴 可选：GitHub OAuth Client ID
GITHUB_CLIENT_SECRET=**your-github-client-secret**  # 🔴 可选：GitHub OAuth Client Secret
NEXT_PUBLIC_APP_URL=http://localhost:3000  # 🔴 必需：应用 URL (开发环境)
```

**配置步骤：**
1. 访问 [GitHub Developer Settings](https://github.com/settings/developers)
2. OAuth Apps → New OAuth App
3. 填写信息：
   - Application name: mornFront
   - Homepage URL: `http://localhost:3000` (开发) 或 `https://yourdomain.com` (生产)
   - Authorization callback URL: `http://localhost:3000/api/github/callback`
4. 复制 Client ID 和 Client Secret

---

## 🚀 AI 流式生成特性

### 字符级流式显示
- **实时打字机效果**: 代码一个个字符逐步出现
- **动态进度条**: 20%-95% 根据字符数实时更新
- **闪烁光标**: 真实的编程体验
- **即时复制**: 生成过程中随时复制代码片段

### 多模型支持
- **免费版**: DeepSeek Chat (每月100次请求)
- **基础版**: DeepSeek Chat + Coder (每月1000次请求)
- **专业版**: GPT-4 + Claude 3 Sonnet (每月5000次请求)
- **旗舰版**: 所有模型 + 无限制使用

---

## 📁 环境变量文件创建

### 步骤 1: 创建 .env.local 文件
```bash
# 在项目根目录创建环境变量文件
touch .env.local
```

### 步骤 2: 复制配置模板
将以下内容复制到 `.env.local` 文件中：

```env
# ===========================================
# mornFront MVP - 环境变量配置
# ===========================================

# -------- AI 模型配置 --------
# DeepSeek (必需 - 免费版)
DEEPSEEK_API_KEY=**sk-your-actual-deepseek-api-key**
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_TOKENS=4000
DEEPSEEK_TEMPERATURE=0.7

# OpenAI (可选 - 专业版以上)
OPENAI_API_KEY=**sk-your-openai-api-key**
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic (可选 - 专业版以上)
ANTHROPIC_API_KEY=**sk-ant-your-anthropic-key**
ANTHROPIC_BASE_URL=https://api.anthropic.com

# -------- 数据库配置 --------
# Supabase (推荐 - 用户认证和数据存储)
NEXT_PUBLIC_SUPABASE_URL=**https://your-project-id.supabase.co**
NEXT_PUBLIC_SUPABASE_ANON_KEY=**your-supabase-anon-key**
SUPABASE_SERVICE_ROLE_KEY=**your-service-role-key**

# -------- 支付配置 --------
# Stripe (推荐 - 国际支付)
STRIPE_SECRET_KEY=**sk_test_your-stripe-secret-key**
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=**pk_test_your-publishable-key**
STRIPE_WEBHOOK_SECRET=**whsec_your-webhook-secret**

# PayPal (可选 - 中文支付)
PAYPAL_CLIENT_ID=**your-paypal-client-id**
PAYPAL_CLIENT_SECRET=**your-paypal-client-secret**
PAYPAL_ENVIRONMENT=sandbox
PAYPAL_WEBHOOK_ID=**your-paypal-webhook-id**
PAYPAL_SKIP_SIGNATURE_VERIFICATION=false

# -------- GitHub 集成 --------
# GitHub OAuth (可选 - 代码推送功能)
GITHUB_CLIENT_ID=**your-github-client-id**
GITHUB_CLIENT_SECRET=**your-github-client-secret**
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 步骤 3: 替换占位符
将所有标有 `**...**` 的占位符替换为你的实际密钥。

### 步骤 4: 重启应用
```bash
# 停止当前服务器 (Ctrl+C)
# 重新启动
npm run dev
```

---

## 🔍 验证配置

### 运行配置检查
```bash
# 检查环境变量配置状态
node test-env.js

# 检查 Supabase 连接
node check-supabase.js

# 检查支付配置
node check-payment-config.js
```

### 测试端点
- **环境变量状态**: `http://localhost:3000/api/test-env`
- **Supabase 连接**: `http://localhost:3000/api/supabase-test`
- **代码生成测试**: `http://localhost:3000/api/generate-stream`
- **支付测试**: `http://localhost:3000/debug/webhook`

---

## 📋 环境变量详细说明

### AI 模型变量

| 变量名 | 必需 | 默认值 | 说明 |
|--------|------|--------|------|
| `DEEPSEEK_API_KEY` | ✅ | 无 | DeepSeek API 密钥 |
| `DEEPSEEK_BASE_URL` | ❌ | `https://api.deepseek.com` | API 基础 URL |
| `DEEPSEEK_MAX_TOKENS` | ❌ | `4000` | 最大生成 token 数 |
| `DEEPSEEK_TEMPERATURE` | ❌ | `0.7` | 生成随机性 (0.0-1.0) |
| `OPENAI_API_KEY` | ❌ | 无 | OpenAI API 密钥 |
| `ANTHROPIC_API_KEY` | ❌ | 无 | Anthropic API 密钥 |

### 数据库变量

| 变量名 | 必需 | 公开 | 说明 |
|--------|------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ | ✅ | Supabase 项目 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ | ✅ | 公共 API 密钥 |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ | ❌ | 服务角色密钥 |

### 支付变量

| 变量名 | 必需 | 公开 | 说明 |
|--------|------|------|------|
| `STRIPE_SECRET_KEY` | ✅ | ❌ | Stripe Secret Key |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | ✅ | ✅ | Stripe Publishable Key |
| `STRIPE_WEBHOOK_SECRET` | ✅ | ❌ | Stripe Webhook Secret |
| `PAYPAL_CLIENT_ID` | ❌ | ❌ | PayPal Client ID |
| `PAYPAL_CLIENT_SECRET` | ❌ | ❌ | PayPal Client Secret |

### GitHub 变量

| 变量名 | 必需 | 公开 | 说明 |
|--------|------|------|------|
| `GITHUB_CLIENT_ID` | ❌ | ❌ | GitHub OAuth Client ID |
| `GITHUB_CLIENT_SECRET` | ❌ | ❌ | GitHub OAuth Client Secret |
| `NEXT_PUBLIC_APP_URL` | ✅ | ✅ | 应用基础 URL |

---

## 🎯 订阅等级功能对比

| 功能 | 免费版 | 基础版 ($9.99/月) | 专业版 ($29.99/月) | 旗舰版 ($99.99/月) |
|------|--------|------------------|------------------|------------------|
| **AI 模型** | | | | |
| DeepSeek Chat | ✅ | ✅ | ✅ | ✅ |
| DeepSeek Coder | ❌ | ✅ | ✅ | ✅ |
| GPT-4 | ❌ | ❌ | ✅ | ✅ |
| GPT-4 Turbo | ❌ | ❌ | ❌ | ✅ |
| Claude 3 Sonnet | ❌ | ❌ | ✅ | ✅ |
| Claude 3 Opus | ❌ | ❌ | ❌ | ✅ |
| **使用限制** | | | | |
| 请求次数/天 | 10 | 50 | 200 | 无限制 |
| 请求次数/月 | 100 | 1000 | 5000 | 无限制 |
| Token 限制/请求 | 2048 | 4096 | 8192 | 32768 |
| **高级功能** | | | | |
| 用户认证 | ❌ | ✅ | ✅ | ✅ |
| 项目保存 | ❌ | ✅ | ✅ | ✅ |
| GitHub 推送 | ❌ | ✅ | ✅ | ✅ |
| 技术支持 | 社区 | 邮件 | 优先 | 专属 |

---

## 🚨 故障排除指南

### 问题：AI 代码生成失败
**错误**: "API key not configured"

**解决方案**:
1. 检查 `DEEPSEEK_API_KEY` 是否正确设置
2. 验证 API 密钥格式 (以 `sk-` 开头)
3. 确认账户有足够余额
4. 重启开发服务器

### 问题：Supabase 连接失败
**错误**: "Invalid API key"

**解决方案**:
1. 验证 `NEXT_PUBLIC_SUPABASE_URL` 格式
2. 检查 API 密钥是否正确
3. 确认 Supabase 项目处于活跃状态
4. 运行数据库迁移脚本

### 问题：支付功能不工作
**错误**: "Stripe not initialized"

**解决方案**:
1. 确认 `STRIPE_SECRET_KEY` 以 `sk_test_` 开头
2. 验证 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` 以 `pk_test_` 开头
3. 检查 webhook 配置是否正确
4. 查看 Stripe Dashboard 的 webhook 事件

### 问题：GitHub 推送失败
**错误**: "GitHub OAuth not configured"

**解决方案**:
1. 检查 GitHub OAuth App 配置
2. 验证回调 URL 是否正确
3. 确认 `GITHUB_CLIENT_ID` 和 `GITHUB_CLIENT_SECRET` 已设置
4. 检查数据库中的 GitHub token 表

---

## ✅ 快速开始检查清单

### 必需配置 (核心功能)
- [ ] 创建 `.env.local` 文件
- [ ] 配置 DeepSeek API 密钥
- [ ] 配置 Supabase 数据库
- [ ] 配置 Stripe 支付
- [ ] 设置 `NEXT_PUBLIC_APP_URL`

### 可选配置 (扩展功能)
- [ ] 配置 OpenAI API 密钥 (专业版)
- [ ] 配置 Anthropic API 密钥 (专业版)
- [ ] 配置 PayPal 支付
- [ ] 配置 GitHub OAuth

### 测试验证
- [ ] 运行 `node test-env.js` 检查配置
- [ ] 访问应用首页测试基本功能
- [ ] 测试 AI 代码生成
- [ ] 测试用户注册/登录 (如果配置了 Supabase)
- [ ] 测试支付功能 (如果配置了 Stripe)

---

## 🔒 安全最佳实践

### API 密钥管理
- ✅ 不要将 `.env.local` 提交到版本控制
- ✅ 使用环境特定的 API 密钥
- ✅ 定期轮换 API 密钥
- ✅ 不要在客户端代码中使用服务角色密钥

### 生产环境部署
- ✅ 使用生产环境的 API 密钥
- ✅ 配置 HTTPS 和安全头部
- ✅ 设置适当的 CORS 策略
- ✅ 启用数据库备份

---

## 📞 技术支持

如遇到配置问题，请：
1. 检查浏览器开发者工具的控制台错误
2. 查看服务器日志输出
3. 运行 `node test-env.js` 检查配置状态
4. 访问 `/debug/webhook` 页面进行诊断

---

**最后更新**: 2025年12月17日
**版本**: v1.0.0












